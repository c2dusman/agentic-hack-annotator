---
phase: 04-pipeline-wiring-frontend-deploy
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - server.js
  - public/index.html
  - public/style.css
  - public/app.js
autonomous: true
requirements:
  - PIPE-01
  - UI-01
  - UI-02
  - UI-03
  - UI-04
  - REL-04

must_haves:
  truths:
    - "POST /api/generate accepts {url, focus} and returns {imageUrl} after running the full pipeline"
    - "Invalid URL returns 400 with error message before any API calls"
    - "node-cron runs cleanupOldFiles() every 10 minutes and once on startup"
    - "Web UI has URL input, optional focus input, and Generate button"
    - "Loading state cycles through 4 emoji stage labels at 5s intervals"
    - "After 30s without response, long-running notice appears"
    - "Success response displays image and Download button"
    - "Error response displays red-bordered error box with server message"
  artifacts:
    - path: "server.js"
      provides: "POST /api/generate route + cron cleanup wiring"
      contains: "app.post('/api/generate'"
    - path: "public/index.html"
      provides: "Full page markup with form, loading, result, and error sections"
      contains: "generate-form"
    - path: "public/style.css"
      provides: "Dark theme styles matching UI-SPEC"
      contains: "#0D0D0D"
    - path: "public/app.js"
      provides: "Form handler, fetch, loading cycle, result/error display"
      contains: "setInterval"
  key_links:
    - from: "public/app.js"
      to: "/api/generate"
      via: "fetch POST with JSON body"
      pattern: "fetch.*api/generate"
    - from: "server.js"
      to: "src/screenshot.js, src/analyze.js, src/annotate.js, src/render.js"
      via: "require + sequential await"
      pattern: "captureScreenshot.*analyzeScreenshot.*generateAnnotations.*renderCard"
    - from: "server.js"
      to: "src/utils.js cleanupOldFiles"
      via: "node-cron schedule"
      pattern: "cron\\.schedule.*cleanupOldFiles"
---

<objective>
Wire the complete AnnotatorAI pipeline and build the web frontend.

Purpose: Connect all four existing pipeline services (screenshot, analyze, annotate, render) into a single POST /api/generate route, add cron-based file cleanup, and build the vanilla HTML/CSS/JS frontend with loading states, result display, and error handling per the UI-SPEC contract.

Output: A fully functional local app -- enter a URL, get an annotated image displayed with download link.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-pipeline-wiring-frontend-deploy/04-CONTEXT.md
@.planning/phases/04-pipeline-wiring-frontend-deploy/04-RESEARCH.md
@.planning/phases/04-pipeline-wiring-frontend-deploy/04-UI-SPEC.md

@server.js
@src/utils.js
@src/screenshot.js
@src/analyze.js
@src/annotate.js
@src/render.js
@public/index.html
@public/style.css
@public/app.js

<interfaces>
From src/screenshot.js:
  captureScreenshot(url) => Promise of { buffer, base64 }

From src/analyze.js:
  analyzeScreenshot(base64, focus) => Promise of { pageTitle, pageTopic, detectedFocus, elements[] }

From src/annotate.js:
  generateAnnotations(analysisData, focus) => Promise of { cardTitle, cardSubtitle, steps[] }

From src/render.js:
  renderCard(annotationData, screenshotBase64, focus, pageUrl) => Promise of { filename, filepath }

From src/utils.js:
  isValidUrl(string) => boolean
  sanitizeFocus(focus) => string or null
  cleanupOldFiles() => void
  ensureOutputDir() => void
  withTimeout(promise, ms, label) => Promise
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire POST /api/generate route and cron cleanup in server.js</name>
  <files>server.js</files>
  <read_first>
    - server.js (current state: Express 5 app with health check, static serving, error handler)
    - src/utils.js (isValidUrl, sanitizeFocus, cleanupOldFiles, withTimeout signatures)
    - src/screenshot.js (captureScreenshot signature)
    - src/analyze.js (analyzeScreenshot signature)
    - src/annotate.js (generateAnnotations signature)
    - src/render.js (renderCard signature)
    - .planning/phases/04-pipeline-wiring-frontend-deploy/04-RESEARCH.md (Pattern 1 and Pattern 2 code examples)
  </read_first>
  <action>
Modify server.js to add the POST /api/generate route and node-cron cleanup. Keep all existing code (health check, static serving, error handler) intact.

Add requires at the top of server.js (after existing requires). Replace the existing single-function utils require with a multi-function destructure:

```javascript
const { captureScreenshot } = require('./src/screenshot');
const { analyzeScreenshot } = require('./src/analyze');
const { generateAnnotations } = require('./src/annotate');
const { renderCard } = require('./src/render');
const cron = require('node-cron');
const { isValidUrl, sanitizeFocus, cleanupOldFiles, ensureOutputDir, withTimeout } = require('./src/utils');
```

Remove the old `const { ensureOutputDir } = require('./src/utils');` line since the new line above replaces it.

Add POST /api/generate route BETWEEN the health check route and the error handler middleware:

```javascript
app.post('/api/generate', async (req, res) => {
  const { url, focus: rawFocus } = req.body;

  if (!url || !isValidUrl(url)) {
    return res.status(400).json({ error: 'Please enter a valid URL starting with http:// or https://' });
  }

  const focus = sanitizeFocus(rawFocus);

  try {
    const filename = await withTimeout(
      (async () => {
        const { base64 } = await captureScreenshot(url);
        const analysisData = await analyzeScreenshot(base64, focus);
        const annotationData = await generateAnnotations(analysisData, focus);
        const { filename } = await renderCard(annotationData, base64, focus, url);
        return filename;
      })(),
      60000,
      'Generation'
    );

    res.json({ imageUrl: `/output/${filename}` });
  } catch (err) {
    console.error(`[${new Date().toISOString()}] Generation error for ${url}:`, err.message);
    res.status(500).json({ error: err.message });
  }
});
```

Add cron cleanup AFTER the existing `ensureOutputDir()` call and BEFORE `app.listen`:

```javascript
cleanupOldFiles();
cron.schedule('*/10 * * * *', () => {
  cleanupOldFiles();
});
```

Per D-03: node-cron every 10 minutes + once on startup.
Per D-08: Server-side URL validation via isValidUrl() before pipeline starts, returns 400 immediately.
Per D-06: PORT already defaults to 3001 via ecosystem.config.js env block.

The route uses withTimeout at 60000ms per brief Section 8.1. Stage-specific errors from each pipeline module bubble up naturally via err.message. Express 5 handles async errors but we catch inline for JSON response format per Pitfall 4 in research.
  </action>
  <verify>
    <automated>node -c server.js</automated>
  </verify>
  <acceptance_criteria>
    - server.js contains `app.post('/api/generate'`
    - server.js contains `require('./src/screenshot')`
    - server.js contains `require('./src/analyze')`
    - server.js contains `require('./src/annotate')`
    - server.js contains `require('./src/render')`
    - server.js contains `require('node-cron')`
    - server.js contains `cron.schedule('*/10 * * * *'`
    - server.js contains `cleanupOldFiles()` called before cron.schedule (startup cleanup)
    - server.js contains `withTimeout(` with `60000`
    - server.js contains `isValidUrl(url)` check returning 400
    - server.js contains `sanitizeFocus(rawFocus)`
    - server.js contains `res.json({ imageUrl:` on success path
    - server.js contains `res.status(500).json({ error: err.message })` on error path
    - `node -c server.js` exits 0 (valid syntax)
  </acceptance_criteria>
  <done>POST /api/generate route wires all 4 pipeline services sequentially with 60s timeout, URL validation returns 400 for invalid URLs, cron cleanup runs every 10 minutes and once on startup, server.js loads without syntax errors.</done>
</task>

<task type="auto">
  <name>Task 2: Build complete frontend (index.html + style.css + app.js)</name>
  <files>public/index.html, public/style.css, public/app.js</files>
  <read_first>
    - public/index.html (current stub)
    - public/style.css (current stub)
    - public/app.js (current stub)
    - .planning/phases/04-pipeline-wiring-frontend-deploy/04-UI-SPEC.md (complete visual and interaction contract)
    - .planning/phases/04-pipeline-wiring-frontend-deploy/04-RESEARCH.md (Pattern 3 and Pattern 4 code examples)
    - .planning/phases/04-pipeline-wiring-frontend-deploy/04-CONTEXT.md (locked decisions D-01, D-02, D-07)
  </read_first>
  <action>
Replace all three frontend stub files with complete implementations per the UI-SPEC contract.

**public/index.html** -- Replace the entire stub with complete markup:
- DOCTYPE html, lang="en", charset UTF-8, viewport meta tag for mobile
- Google Fonts link for DM Sans weights 400 and 600: `https://fonts.googleapis.com/css2?family=DM+Sans:wght@400;600&amp;display=swap`
- Title: AnnotatorAI
- Link to style.css in head
- Body contains a single `main` element with class `container` (max-width 600px centered)
- Inside container, in order:
  1. `h1` with text "AnnotatorAI"
  2. `p` with class `subtitle` and text "Paste any URL. Get an annotated visual. Instantly."
  3. `form` with id `generate-form` containing:
     - Field group div: `label[for=url-input]` "URL" + `input[type=url][id=url-input][placeholder=https://example.com][required]`
     - Field group div: `label[for=focus-input]` "Focus (optional)" + `input[type=text][id=focus-input][placeholder=e.g. How to connect Gmail, Setting up API keys]` + `p.helper-text` "Leave blank and AI will decide what to annotate"
     - `button[type=submit][id=generate-btn]` "Generate Image"
     - `p[id=inline-error].inline-error[hidden]` (empty, filled by JS)
  4. `div[id=loading-section][hidden]` containing: `div.spinner` + `p[id=status-text].status-text` + `p[id=long-running-notice].long-running-notice[hidden]` "Taking longer than usual..."
  5. `div[id=result-section][hidden]` containing: `img[id=result-image][alt=Generated annotation]` + `a[id=download-btn].download-btn` "Download Image"
  6. `div[id=error-section].error-box[hidden]` containing: `p[id=error-text]`
- Script tag at end of body: `script[src=app.js]`

**public/style.css** -- Replace entirely with dark theme styles per UI-SPEC:

Reset: `*, *::before, *::after { box-sizing: border-box; margin: 0; padding: 0; }`

body: font-family 'DM Sans' sans-serif, background #0D0D0D, color #FFFFFF, min-height 100vh, padding 64px 16px

.container: max-width 600px, margin 0 auto

h1: font-size 36px, font-weight 600, line-height 1.2, margin-bottom 8px

.subtitle: font-size 16px, font-weight 400, color #CCCCCC, margin-bottom 32px

form: background #1A1A1A, border-radius 12px, padding 24px, display flex, flex-direction column, gap 16px

label: font-size 16px, font-weight 600, line-height 1.4, display block, margin-bottom 4px

input[type="url"] and input[type="text"]: width 100%, font-family 'DM Sans' sans-serif, font-size 18px, font-weight 400, line-height 1.4, color #FFFFFF, background #1A1A1A, border 1px solid rgba(255,255,255,0.15), border-radius 8px, padding 12px 16px, outline none, transition border-color 150ms ease

input focus states: border-color #FF2D6B

input::placeholder: color rgba(255,255,255,0.3)

.helper-text: font-size 14px, font-weight 400, color #CCCCCC, line-height 1.5, margin-top -8px

#generate-btn: width 100%, font-family 'DM Sans' sans-serif, font-size 18px, font-weight 600, color #FFFFFF, background #FF2D6B, border none, border-radius 8px, padding 16px 24px, cursor pointer, transition background-color 150ms ease, margin-top 8px. Hover: background #E0245E. Disabled: opacity 0.5, cursor not-allowed.

.inline-error: font-size 14px, color #FF4444, margin-top -8px

#loading-section: text-align center, margin-top 48px

.spinner: width 40px, height 40px, border 3px solid rgba(255,45,107,0.2), border-top-color #FF2D6B, border-radius 50%, animation spin 0.8s linear infinite, margin 0 auto 16px

@keyframes spin: to transform rotate(360deg)

.status-text: font-size 16px, font-weight 400, color #CCCCCC

.long-running-notice: font-size 14px, color #CCCCCC, margin-top 8px

#result-section: margin-top 48px, text-align center

#result-image: width 100%, border-radius 8px, margin-bottom 16px

.download-btn: display inline-block, font-family 'DM Sans' sans-serif, font-size 18px, font-weight 600, color #FF2D6B, background transparent, border 2px solid #FF2D6B, border-radius 8px, padding 16px 24px, text-decoration none, transition background-color 150ms ease and color 150ms ease. Hover: background #FF2D6B, color #FFFFFF.

.error-box: margin-top 48px, border 2px solid #FF4444, background rgba(255,68,68,0.08), border-radius 8px, padding 16px

.error-box p: font-size 14px, font-weight 400, color #CCCCCC, line-height 1.5

**public/app.js** -- Replace entirely with interaction logic:

DOM element references by getElementById: generate-form, url-input, focus-input, generate-btn, loading-section, result-section, error-section, status-text, long-running-notice, result-image, download-btn, error-text, inline-error

STAGES array with 4 entries (exact emoji + text from UI-SPEC Copywriting Contract):
  Index 0: camera emoji + " Capturing screenshot..."
  Index 1: magnifying glass emoji + " Analyzing page..."
  Index 2: writing hand emoji + " Writing annotations..."
  Index 3: palette emoji + " Rendering image..."

isValidUrlClient(str) function: try new URL(str), return protocol is http: or https:, catch returns false

showInlineError(msg): set inlineError.textContent = msg, inlineError.hidden = false
hideInlineError(): inlineError.hidden = true

Module-level variables: stageIndex = 0, stageInterval = null, longRunningTimeout = null

startLoading(): disable generateBtn, show loadingSection, hide resultSection and errorSection and longRunningNotice, hideInlineError, set stageIndex = 0, set statusText.textContent = STAGES[0], start stageInterval = setInterval every 5000ms advancing stageIndex = (stageIndex + 1) % STAGES.length and updating statusText.textContent, start longRunningTimeout = setTimeout at 30000ms that sets longRunningNotice.hidden = false

stopLoading(): clearInterval(stageInterval), clearTimeout(longRunningTimeout), hide loadingSection, enable generateBtn

showResult(imageUrl): set resultImage.src = imageUrl, set downloadBtn.href = imageUrl, set downloadBtn.download = imageUrl.split('/').pop(), show resultSection

showError(message): set errorText.textContent = message, show errorSection

form submit event listener (async, e.preventDefault()):
  1. hideInlineError()
  2. Get url = urlInput.value.trim(), focus = focusInput.value.trim()
  3. If !url: showInlineError('URL is required'), return
  4. If !isValidUrlClient(url): showInlineError('Please enter a valid URL starting with http:// or https://'), return
  5. startLoading()
  6. try: fetch POST /api/generate with Content-Type application/json, body JSON.stringify({ url, focus: focus || undefined })
  7. Parse response as JSON
  8. If !response.ok: showError(data.error || 'Generation failed.')
  9. Else: showResult(data.imageUrl)
  10. catch: showError('Network error. Please try again.')
  11. finally: stopLoading()

Key decisions implemented:
- D-01: setInterval cycling through 4 emoji labels every 5000ms (per brief Section 9)
- D-02: setTimeout at 30000ms shows "Taking longer than usual..." notice
- D-07: Red-bordered error box (2px solid #FF4444, rgba background), no retry button
- D-08: Client-side URL validation mirrors server-side isValidUrl() logic (discretion item)
- UI-SPEC: All colors, typography, spacing, and copy match the contract exactly
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'); const h=fs.readFileSync('public/index.html','utf8'); const c=fs.readFileSync('public/style.css','utf8'); const j=fs.readFileSync('public/app.js','utf8'); let ok=true; const checks=[['HTML form',h.includes('generate-form')],['HTML loading',h.includes('loading-section')],['HTML result',h.includes('result-section')],['HTML error',h.includes('error-section')],['CSS dark bg',c.includes('#0D0D0D')],['CSS accent',c.includes('#FF2D6B')],['CSS spinner',c.includes('@keyframes spin')],['JS fetch',j.includes('/api/generate')],['JS setInterval',j.includes('setInterval')],['JS 30s timeout',j.includes('30000')]]; checks.forEach(([n,v])=>{console.log(n+':',v); if(!v) ok=false;}); if(!ok) process.exit(1);"</automated>
  </verify>
  <acceptance_criteria>
    - public/index.html contains `generate-form` id
    - public/index.html contains `loading-section` id
    - public/index.html contains `result-section` id
    - public/index.html contains `error-section` id
    - public/index.html contains `url-input` id
    - public/index.html contains `focus-input` id
    - public/index.html contains `generate-btn` id
    - public/index.html contains Google Fonts link with `DM+Sans`
    - public/index.html contains `AnnotatorAI` in h1
    - public/style.css contains `#0D0D0D` (page background)
    - public/style.css contains `#FF2D6B` (accent color)
    - public/style.css contains `#1A1A1A` (card background)
    - public/style.css contains `#FF4444` (error border)
    - public/style.css contains `@keyframes spin` (spinner animation)
    - public/style.css contains `max-width: 600px` (container)
    - public/app.js contains `fetch('/api/generate'` or `fetch("/api/generate"`
    - public/app.js contains `setInterval` (stage cycling per D-01)
    - public/app.js contains `5000` (stage interval ms)
    - public/app.js contains `30000` (long-running timeout per D-02)
    - public/app.js contains `Taking longer than usual` or references long-running-notice element
    - public/app.js contains `isValidUrlClient` (client-side validation)
    - public/app.js contains `showError` and `showResult` functions
    - public/app.js contains `download` attribute assignment for download button
  </acceptance_criteria>
  <done>All three frontend files implement the complete UI-SPEC contract: dark-themed form with URL and focus inputs, Generate button, loading state with 4 emoji stage labels cycling at 5s intervals, 30s long-running notice, result display with image and download link, and red-bordered error box. No stubs or placeholders remain.</done>
</task>

</tasks>

<verification>
1. `node -c server.js` passes (valid syntax)
2. All DOM element IDs referenced in app.js exist in index.html
3. All CSS class names used in index.html are defined in style.css
4. server.js POST /api/generate route calls all 4 pipeline functions in order
5. app.js POSTs to /api/generate and handles both success and error responses
</verification>

<success_criteria>
- server.js has a working POST /api/generate route that wires captureScreenshot, analyzeScreenshot, generateAnnotations, and renderCard sequentially with a 60s timeout
- server.js runs cleanupOldFiles() on startup and every 10 minutes via node-cron
- Frontend displays a dark-themed form at max-width 600px with URL input, focus input, and Generate button
- Loading state shows 4 emoji stage labels cycling at 5s intervals
- Success response shows the generated image with a Download button
- Error response shows a red-bordered error box with the server message
- All code is vanilla HTML/CSS/JS with no framework dependencies
</success_criteria>

<output>
After completion, create `.planning/phases/04-pipeline-wiring-frontend-deploy/04-01-SUMMARY.md`
</output>
