---
phase: 02-screenshot-ai-services
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - src/annotate.js
autonomous: true
gap_closure: true
requirements:
  - PIPE-04
  - FOCUS-03

must_haves:
  truths:
    - "Steps array length equals the number of elements in the analysis input"
    - "Both FOCUS_PROMPT and NO_FOCUS_PROMPT contain explicit one-step-per-element instruction"
  artifacts:
    - path: "src/annotate.js"
      provides: "Claude Sonnet copywriting with step-count constraint"
      exports: ["generateAnnotations"]
      contains: "Create exactly one step for each element"
  key_links:
    - from: "src/annotate.js FOCUS_PROMPT"
      to: "elements array in analysis JSON"
      via: "prompt instruction constraining step count"
      pattern: "exactly one step for each element"
    - from: "src/annotate.js NO_FOCUS_PROMPT"
      to: "elements array in analysis JSON"
      via: "prompt instruction constraining step count"
      pattern: "exactly one step for each element"
---

<objective>
Fix the single verification gap: add explicit step-count-to-element-count instruction to both Claude prompt templates in annotate.js.

Purpose: The verification found that neither FOCUS_PROMPT nor NO_FOCUS_PROMPT tells Claude to produce exactly one step per element. Without this, Claude generates an arbitrary number of steps that may not match the Gemini elements array length, breaking the Phase 3 card template layout.
Output: Updated src/annotate.js with both prompts containing the one-step-per-element constraint.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-screenshot-ai-services/02-CONTEXT.md
@.planning/phases/02-screenshot-ai-services/02-VERIFICATION.md
@.planning/phases/02-screenshot-ai-services/02-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add one-step-per-element instruction to both prompt templates</name>
  <files>src/annotate.js</files>
  <read_first>src/annotate.js, .planning/phases/02-screenshot-ai-services/02-VERIFICATION.md</read_first>
  <action>
Edit src/annotate.js to add an explicit step-count constraint to both FOCUS_PROMPT and NO_FOCUS_PROMPT. The instruction must appear in the Rules section of each prompt template.

**In FOCUS_PROMPT**, add this line to the Rules block (after the existing rules, before the "Return ONLY valid JSON" rule):
```
- Create exactly one step for each element in the elements array — use the element label and description as the basis for that step. The number of steps MUST equal the number of elements.
```

**In NO_FOCUS_PROMPT**, add this identical line to its Rules block (same position — after existing rules, before the "Return ONLY valid JSON" rule):
```
- Create exactly one step for each element in the elements array — use the element label and description as the basis for that step. The number of steps MUST equal the number of elements.
```

The exact insertion points:
- FOCUS_PROMPT: Insert after `- Keep it punchy and clear` (currently line 33) and before `- Return ONLY valid JSON. No explanation. No markdown.` (currently line 34)
- NO_FOCUS_PROMPT: Insert after `- Keep it punchy and clear` (currently line 59) and before `- Return ONLY valid JSON. No explanation. No markdown.` (currently line 60)

Do NOT change anything else in the file — no model string changes, no function signature changes, no other prompt wording changes. This is a surgical one-line-per-prompt addition.
  </action>
  <verify>
    <automated>cd "/Users/usman/Library/CloudStorage/GoogleDrive-mohammed.usman.wahid@gmail.com/My Drive/C2D Inc/Claude Code/Agentic Academy Hackathon/Annotator" && node -e "const c = require('fs').readFileSync('src/annotate.js','utf8'); const focus = c.indexOf('FOCUS_PROMPT'); const nofocus = c.indexOf('NO_FOCUS_PROMPT'); const pat = 'exactly one step for each element'; const count = (c.match(new RegExp(pat, 'g')) || []).length; console.log('occurrences:', count); console.log(count === 2 ? 'PASS' : 'FAIL')"</automated>
  </verify>
  <acceptance_criteria>
    - src/annotate.js contains the string "exactly one step for each element" exactly 2 times (once in FOCUS_PROMPT, once in NO_FOCUS_PROMPT)
    - src/annotate.js contains "number of steps MUST equal the number of elements" exactly 2 times
    - src/annotate.js still contains `require('@anthropic-ai/sdk')` (no accidental deletion)
    - src/annotate.js still exports `generateAnnotations` (no accidental deletion)
    - src/annotate.js still contains `model: 'claude-sonnet-4-5'` (no model string change)
    - `node -e "const a = require('./src/annotate'); console.log(typeof a.generateAnnotations)"` outputs "function"
  </acceptance_criteria>
  <done>Both FOCUS_PROMPT and NO_FOCUS_PROMPT contain explicit instruction to create exactly one step per element, guaranteeing step count matches element count from Gemini analysis</done>
</task>

</tasks>

<verification>
1. `grep -c "exactly one step for each element" src/annotate.js` outputs `2`
2. `grep -c "number of steps MUST equal" src/annotate.js` outputs `2`
3. `node -e "const a = require('./src/annotate'); console.log(typeof a.generateAnnotations)"` outputs `function`
4. No other lines changed: `git diff src/annotate.js` shows only 2 added lines (one per prompt)
</verification>

<success_criteria>
- Both prompt templates instruct Claude to produce exactly one step per element from the analysis input
- No other code in annotate.js was modified
- Module still loads and exports generateAnnotations correctly
</success_criteria>

<output>
After completion, create `.planning/phases/02-screenshot-ai-services/02-04-SUMMARY.md`
</output>
